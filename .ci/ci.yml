# CI configuration — parked (not yet wired to a CI provider)
# Intended workflow:
#
#   1. Install: pip install -e ".[dev]"
#   2. Lint:    ruff check src tests
#   3. Test:    pytest
#
# No network access required; all tests run offline.
#
# Future: wire to GitHub Actions, GitLab CI, or similar.
# Example GitHub Actions stub:
#
# name: ci
# on: [push, pull_request]
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-python@v5
#         with: { python-version: "3.11" }
#       - run: pip install -e ".[dev]"
#       - run: ruff check src tests
#       - run: pytest
